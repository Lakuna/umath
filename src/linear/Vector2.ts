import type Vector from "./Vector.js";

/** A quantity with magnitude and direction in two dimensions. */
export type Vector2Like = [number, number];

/** A quantity with magnitude and direction in two dimensions. */
export default class Vector2 extends Float32Array implements Vector {
	// TODO
}
